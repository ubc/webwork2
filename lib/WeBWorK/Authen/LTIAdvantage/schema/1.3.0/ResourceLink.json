{
	"$schema": "http://json-schema.org/draft-06/schema#",
	"$id": "ResourceLink.json",
	"title": "ResourceLink",
	"description": "A link to tool's resource from the tool's platform",
	"properties": {
	  "https://purl.imsglobal.org/spec/lti/claim/resource_link": {
		"type": "object",
		"properties": {
		  "id": {
			"description":
			  "This is an opaque unique within the tool platform identifier for that resource link. Uniqueness MUST be enforced within the platform.",
			"type": "string"
		  },
		  "title": {
			"description":
			  "A plain text title for the resource. This is the clickable text that appears in the link.",
			"type": ["string", "null"]
		  },
		  "description": {
			"description":
			  "A plain text description of the link’s destination, suitable for display alongside the link.",
			"type": ["string", "null"]
		  }
		},
		"required": ["id"]
	  }
	},
	"required": ["https://purl.imsglobal.org/spec/lti/claim/resource_link"]
  }
